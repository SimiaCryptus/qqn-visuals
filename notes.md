The thumbnails for the landscape types are all messed up - a few misaligned rows of pixels are bright.
Add console logging and diagnostics/defensive code
the first illustration - the qqn simulator - is locked on rosenbrock. rosenbrock is the best default but it should obey settings.
the illustration at `3 · x ← x − η∇f` should have configurable # of stopping iterations.
`4 · Momentum & Adam` and after - we see multiple redundant control panels
